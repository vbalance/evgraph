# PostgreSQL Remote Database Configuration
POSTGRES_DB=your_database_name
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password
POSTGRES_HOST=your_remote_host
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8174
BACKEND_HOST=0.0.0.0

# Frontend Configuration (must have VITE_ prefix for browser access)
# Production (via nginx proxy): /api
# Local development (direct): http://localhost:8174/api
VITE_API_BASE_URL=/api

# Application Settings
ENV=production
DEBUG=false