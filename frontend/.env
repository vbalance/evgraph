# Frontend Environment Variables
# Note: All variables must have VITE_ prefix to be accessible in the browser

# API Base URL
# For Docker/production: use relative path (nginx proxy)
VITE_API_BASE_URL=/api

# For local development without Docker, create .env.local with:
# VITE_API_BASE_URL=http://localhost:8174/api