# Frontend Environment Variables
# Note: All variables must have VITE_ prefix to be accessible in the browser

# API Base URL
# Production (via nginx proxy): /api
# Local development (direct to backend): http://localhost:8174/api
VITE_API_BASE_URL=/api